{
  "name": "turbo-mono-repo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@9.6.7",
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start --no-daemon",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}