{
  "name": "turbo-mono-repo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "pnpm@10.5.0",
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start --no-daemon",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}