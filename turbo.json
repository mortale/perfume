{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "start": {
      "cache": false
    },
    "build": {
      "outputs": ["dist/**", ".next/**", "build/**"],
      "dependsOn": ["^build"]
    }
  }
}